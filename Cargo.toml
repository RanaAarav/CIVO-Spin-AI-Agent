[package]
name = "civo-spin-ai-agent"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# The Spin SDK allows us to talk to the host (Serverless)
spin-sdk = "3.0" 
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"